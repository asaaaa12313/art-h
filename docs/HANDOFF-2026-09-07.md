# 이어받기 — 아트에이치치과 홈페이지 개편 (2026-09-07 중단)

브랜치: `redesign-2026-09` (main 미푸시) / 최신 커밋 `b6acb5f` (09-08 2차 정합)
기획: `docs/plan-rebuild-2026-09.md` (5차) · 참고 원본 캡처: 아래 「캡처 다시 뜨는 법」

---

## 지금 상태

| 구분 | 상태 |
|---|---|
| 배포 | **안 함.** 로컬 커밋 2개만 있음(`37cc7aa` 법규·사진 / `27f44ff` 개편) |
| 검증 | `npx tsc --noEmit` 오류 0 · 21개 주소 전부 200 (마지막 확인 시점) |
| 검수 | 2회 완료, 지적 28건 반영. 3회차는 **푸시 직전에 다시** |
| 로컬 서버 | 꺼둠. `preview_start`로 `art-h-dev` 실행(포트 3157) |

---

## 다음에 할 일 (우선순위 순)

### 1. 레퍼런스 정합 — 09-08에 아래까지 맞췄다
캡처 대조로 반영 완료(커밋 `b6acb5f`): 우측 레일 파란 상자·전화번호 세로, 콘텐츠 폭 1440,
진료과목 화면고정 가로 갤러리, 특별함 화면끝 사진 + 세로 실선, 하위 페이지 사진띠+남색 제목띠,
하위 페이지 폭 1300, 장비 3:4, 그림 색 네이비 통일, 좁은 화면 로고 복원.

남은 차이(작은 것만):
1. 히어로에 CTA 버튼 4개가 있다 — 레퍼런스는 없다. 예약 전환을 위해 일부러 남겼다(뺄지는 판단 필요).
2. 레퍼런스 히어로의 곡선 글씨(호를 그리는 문장)는 아직 없다.
3. 의료진 구간의 아치형(위가 둥근) 사진 띠 + 원형 「+」 버튼은 아직 카드 형태다.

### 2. 원장님 회신 대기 4건 — 받으면 바로 반영
발송 문안은 `docs/send-to-clinic-2026-09-07.md`에 준비돼 있다(아직 안 보냈으면 이것부터).

| 항목 | 반영할 곳 |
|---|---|
| 부작용·주의사항 문구 검수 | `Treatment.risks` 신설 후 상세 페이지 |
| CCTV 대수·촬영범위·보관기간 | `/privacy` 제9조 신설(지금은 조항 자체를 뺀 상태) |
| 덴트웹 법인명 | `PRIVACY_PROCESSORS[0].name` 한 줄 |
| 진정 안전 장비·교육 이력 | 의식하진정 페이지 SAFETY 블록 |

### 3. 푸시 전 순서
`git fetch` → 검수 1회(누적 diff) → 지적 반영 → 사용자 승인 → `main` 푸시 → Vercel READY 확인.

---

## 캡처 다시 뜨는 법 (이번에 만든 도구)

```bash
python3 ~/.claude/scripts/site-shot.py <URL> <저장폴더> <접두> [뷰포트폭] [최대장수]
```

- 예: `python3 ~/.claude/scripts/site-shot.py https://hnleadersdental.com/ /tmp/ref home 1440 14`
- 우리 사이트도 같은 방식으로 떠서 **나란히 Read**로 비교한다. 내장 미리보기는 창이 숨으면 스크롤 후 흰 화면만 나와 못 쓴다.
- 이번 세션 캡처는 스크래치패드에 있고 세션이 끝나면 사라진다 — 다시 뜨면 된다.

---

## 확정된 결정 (재론 금지)

- 색 = 레퍼런스 실측 네이비 `#10417C` 단일 톤 (미드나잇+민트 안은 폐기)
- 제목 폰트 = 리디바탕 자체 호스팅
- 히어로 = 사진↔영상 교차, 전환 4종 순환, 첫 장면은 **건물 외관**(휴게실 제외)
- 헤더 = 2단, 네이버 예약 버튼은 헤더에서 뺌(우측 레일·하단 바에 있음), 그 자리에 언어 전환
- 다국어 = 전체 번역이 아니라 **언어별 안내 1페이지**(의료 표현 오역 위험)
- 상담 레일 = 폭 96px·아이콘 28px·5항목

## 손대면 안 되는 것

- `.env.local.bak-20260907-131337` — 환경변수 백업. `.gitignore`에 안 걸리니 커밋 금지(삭제 권장).
- `raw-assets/` — 촬영 원본·폰트 원본·영상 원본 백업. git 제외.
