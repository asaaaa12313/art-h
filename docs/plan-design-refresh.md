# 기획 — 경쟁 3사 벤치마킹 디자인 개편 (2026-07-23, ✅ 승인됨)

## 🚀 새 세션 주입 프롬프트 (승인 2026-07-23)
아래를 새 채팅에 붙여넣으면 구현이 시작된다:

> `/Users/kimminwoo/Documents/업무/00.개발/0.claude/homepage/art-h/docs/plan-design-refresh.md` 읽고 P1~P4 구현 시작.
> 목표: 플로팅 상담 레일 + 리뷰 링크·보증제 블록 + 스토리 3밴드 + 과목 카드 실사·워터마크 / 범위: `app/home/Home.tsx`+CSS, `app/layout.tsx`, `lib/copy.ts` / 종료조건: 체크리스트 전부 [x] + `npx tsc --noEmit` PASS + 검수 2봇 Critical/High 0 / 검증: tsc + 프리뷰 실측(모바일 뷰포트 포함)
> 승인된 plan이므로 재논의 금지. 확정 조건: 카카오톡·유튜브·인스타 채널 **없음**(카톡 버튼·P5 피드 제외), 네이버 리뷰는 **링크만**(건수 비노출). 무드 3단어(차분한 신뢰·세리프 품격·잉크 네이비) 준수, 구현 직전 목업 위젯 1회.

## ✅ 승인된 결정 (2026-07-23 사용자 확정)

1. **P1~P4 전부 진행** (P5 콘텐츠 피드는 채널 부재로 자동 제외).
2. **보유 채널 없음** → 플로팅 레일은 전화·네이버예약·오시는길 구성(카톡 버튼 제외).
3. **네이버 리뷰 링크만** — "리뷰 보러가기" 버튼, 건수 비노출.

> 근거: [research-design-3refs.md](research-design-3refs.md) (3사 실측 분석)
> 독자: 결정권자(김민우) — 이 문서만 읽고 승인/반려/항목 선택이 가능하도록 우선순위·비용 표기.

## 기각 대안

- **A안(기각) 광고 랜딩형 전면 전환**: 퍼펙트·미니쉬처럼 다크 배경+후킹 카피 랜딩으로 갈아엎는 안 — 기존 세리프·여백 브랜딩(v1.4x 자산)을 폐기하게 되고, 최상급 카피는 의료광고 심의 리스크. 기각.
- **B안(기각) 다크+네온 포인트 도입**: 퍼펙트식 다크 네이비+라임 — "진료 너머, 사람의 고귀함" 무드와 충돌, 송도 타깃(가족 단위)에 과함. 기각.
- **C안(채택) 아이덴티티 유지 + 구조만 이식**: 세리프·잉크 네이비 톤은 그대로 두고, 3사의 **전환 장치·사회적 증거·스토리 밀도**만 가져온다.

## 무드 3단어 (기존 유지·강화)

**차분한 신뢰 · 세리프 품격 · 잉크 네이비** — 모든 신규 요소는 이 3단어에 대조해 채택.

## 목표 4요소

- **목표**: 전환(예약·전화) 동선과 신뢰 근거를 3사 수준으로 끌어올리되, 아트에이치 고급 톤은 유지.
- **범위**: 홈(`app/home/Home.tsx`+CSS) + 공통 레이아웃(`app/layout.tsx` 플로팅 레일) + `lib/copy.ts`. 과목 상세는 이번 범위 제외(v1.46~49로 이미 개편).
- **종료조건**: 승인된 항목 구현 + `npx tsc --noEmit` PASS + 검수 2봇 Critical/High 0.
- **검증**: tsc + 프리뷰 실측(데스크톱·모바일 뷰포트).

## 제안 항목 (우선순위순 — 개별 채택/제외 가능)

### P1. 플로팅 상담 레일 ⭐ 전환율 직결 (루센트+미니쉬 공통 구조)
- 데스크톱: 우측 고정 세로 레일 — 전화하기 / 네이버 예약 / 카카오톡 채널(보유 시) / 오시는길. 잉크 네이비 바탕+세리프 숫자로 우리 톤 유지.
- 모바일: 하단 고정 바(전화 · 네이버 예약 2버튼) — 엄지 도달 거리 최우선.
- 스크롤 시 항상 노출, 히어로에서는 반투명 → 본문부터 불투명.

### P2. 신뢰 숫자 밴드 업그레이드 (루센트 리뷰 뱃지 + 미니쉬 규모 블록)
- 기존 STATS(멸균 9단계·전문의 2인·7과목·8층)에 **네이버 리뷰 수 뱃지**("리뷰 N건 보러가기" — 네이버 플레이스 링크) 추가.
- "치료 보증제" (copy.ts SYSTEM_ITEMS에 문구 기존재) → 비교 프레임 블록으로 승격: "일반 원칙(보증 없음·구두 안내)" vs "아트에이치(진료 보증서 발급·정기 검진 책임)". 타사 비방 없이 우리 약속만.

### P3. 홈 "왜 아트에이치인가" 풀블리드 스토리 3밴드 (퍼펙트 구조)
- 한 밴드 = 한 메시지, 07.21 실사 배경: ①구강악안면외과 전문의 직접 집도(수술 실사) ②독립 수술실·9단계 멸균(수술실 풀세팅 컷) ③의식하진정 실시간 모니터링(BM1 실사). 크림·네이비 밴드 리듬(v1.48)과 톤 통일.
- 장비·재료 실명은 이미 과목 상세에 있으므로 밴드에서 해당 과목으로 딥링크.

### P4. 홈 진료과목 카드 실사 교체 + 워터마크 타이포 (루센트 "Lucent Special" 구조)
- TX_IMG 7과목 카드 이미지를 07.21 실사로 교체(임플란트=수술컷, 턱관절=TMD 설명컷 등).
- 섹션 배경에 대형 영문 워터마크 "ART H SPECIAL" (세리프, 3% 투명도) — 루센트 구조를 우리 폰트로.

### ~~P5. 콘텐츠 피드~~ (제외 확정 — 2026-07-23)
- 유튜브·인스타 채널 부재로 제외. 빈 피드는 역효과. 채널 개설 시 재검토.

## 작업 체크리스트 (구현 세션에서 갱신)

- [x] P1 플로팅 레일 (데스크톱: 전화·네이버예약·오시는길 / 모바일: 하단 바 2버튼) — 2026-07-23 구현
- [x] P2 네이버 리뷰 "보러가기" 링크(건수 비노출) + 보증제 비교 블록
- [x] P3 스토리 3밴드 (전문의 집도·독립 수술실·진정 모니터링, 07.21 실사 배경)
- [x] P4 과목 카드 7종 실사 교체 + "ART H SPECIAL" 워터마크
- [x] `npx tsc --noEmit` PASS + 프리뷰 실측(모바일 375·데스크톱 1440 — 이미지 10종 200, 콘솔/서버 에러 0)
- [x] 검수 2봇 병렬 → Critical/High 0 (반증·회귀 PASS / 실사용·운영 High 2→수정→재검증 해소, 2026-07-23)
